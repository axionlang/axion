  --> axionc/tests/fixtures/exhaustive_missing.axi:3:10
  = help: add the missing constructor arm(s), or a `_` wildcard catch-all.
  |
  |          ^ this `case` does not cover every constructor
3 | rank c = case c of
error[AX0202]: non-exhaustive patterns: Blue not covered
