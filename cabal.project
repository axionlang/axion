packages: .

-- The dev shell (flake.nix) provides all dependencies inside GHC
-- (ghcWithPackages), so cabal resolves only against the already-installed
-- packages, without touching the Hackage index or the network.
active-repositories: none
